kind: Pod
metadata:
  name: nginx-proxy
  namespace: default
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80

# 动态生成 Nginx 配置（要结合dns.yaml）
# server {
#     listen 80;
#     server_name minik8s.com;
#     location /path1 {
#         proxy_pass http://service1:8080;
#     }
#     location /path2 {
#         proxy_pass http://service2:9090;
#     }
# }
